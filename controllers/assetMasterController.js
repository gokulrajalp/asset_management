// controllers/employeeController.js
const assetMasterController = {
  renderAssetMasterDashboard: (req, res) => {
    res.render('asset_master-dashboard');
  },

  // Additional employee dashboard-related functions if needed
};

module.exports = assetMasterController;