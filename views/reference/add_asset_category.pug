extends layout

block content
  div.container.mt-5
    h1.text-center Add Asset Category
    form(action='/asset_category/add', method='POST')
      div.form-group
        label(for='name') Name:
        input.form-control(type='text', name='name', required=true)
      button.btn.btn-primary(type='submit') Add Asset Category
